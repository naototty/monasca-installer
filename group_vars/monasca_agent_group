---
# file: group_vars/monasca_agent_group

# directories
base_dir_agent: /opt/monasca
logstash_agent_dest: "{{ base_dir_agent }}/monasca-log-agent"
logstash_agent_conf_dir: /etc/monasca/monasca-log-agent
logstash_agent_log_dir: /var/log/monasca/monasca-log-agent

monasca_agent_venv_dir: "{{ venvs_dir }}/monasca-agent"
# that one deprecated use one above
monasca_virtualenv_dir: "{{ monasca_agent_venv_dir }}"
monasca_conf_dir: /etc/monasca
monasca_usr_lib_dir: /usr/lib/monasca/agent

services:
  - { name: "monasca-log-agent", configuration: [ "{{ logstash_agent_conf_dir }}", "{{ logstash_agent_dest }}", "{{ base_dir_agent }}" ] }
