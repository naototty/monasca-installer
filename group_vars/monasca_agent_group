---
# file: group_vars/monasca_agent_group

monsasca_log_api_admin_port: 8071

# directories
base_dir_agent: /opt/monasca

monasca_agent_venv_dir: "{{ venvs_dir }}/monasca-agent"
# that one deprecated use one above
monasca_virtualenv_dir: "{{ monasca_agent_venv_dir }}"
monasca_conf_dir: /etc/monasca
monasca_agent_conf_dir: "{{ monasca_conf_dir }}/agent"
monasca_usr_lib_dir: /usr/lib/monasca/agent

monasca_agent_database_user: monapi
monasca_agent_database_password: "{{ csi_monasca_agent_database_password }}"
monasca_agent_version: 1.1.18.dev1

monasca_agent_overwrite_config: False

elasticsearch_url: "http://localhost:9200"

services:
  - { name: "monasca-agent", configuration: [ "{{monasca_agent_conf_dir}}" ] }

# List of passwords/credentials for verification
credential_keys_list:
  - csi_monasca_agent_keystone_password
  - openstack_monasca_agent_keystone_password
  - csi_monasca_agent_database_password
