---
# Copyright 2016 FUJITSU LIMITED

kafka_log_dir: "{{ log_dir }}/kafka"

kafka_host: "{{ ansible_ssh_host }}"
kafka_listen_address: "{{ kafka_host }}"
kafka_apache_mirror: "{{ world_repo_url }}"

kafka_id: 0
