---
# file: group_vars/monasca_log_agent_group

# logstash agent download url
logstash_agent_download_url: "{{ balls_repo_url }}/world"

# directories
base_dir_agent: /opt/monasca
log_dir: /var/log
logstash_agent_dest: "{{ base_dir_agent }}/monasca-log-agent"
logstash_agent_conf_dir: /etc/monasca/monasca-log-agent
logstash_agent_log_dir: /var/log/monasca/monasca-log-agent
log_files:
  - "{{ logstash_agent_log_dir }}"
  - "{{ log_dir }}/messages"
  - "{{ log_dir }}/yum.log"
  - "{{ log_dir }}/boot.log"

# services
services:
  - { name: "monasca-log-agent", configuration: [ "{{ logstash_agent_conf_dir }}", "{{ logstash_agent_dest }}" ] }

# List of passwords/credentials for verification
credential_keys_list:
  - cmm_monasca_log_agent_keystone_password
  - openstack_monasca_log_agent_keystone_password
