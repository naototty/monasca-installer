---
# file: host_vars/openstack-monasca-agent

# Monasca Agent Configuration
monasca_agent:
  user: admin-agent
  password: admin
  project: admin

monasca_agent_version: 1.1.12.post1
  
monasca_checks:
  host_alive:
    init_config:
      ssh_port: 22
      ssh_timeout: 0.5
      ping_timeout: 1
    instances:
      - name: monasca
        host_name: "{{ monasca_host }}"
        alive_test: ssh
