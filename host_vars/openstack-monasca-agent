---
# file: group_vars/monasca-agent-openstack

# Monasca Agent Configuration
monasca_agent:
  user: admin-agent
  password: admin
  project: admin
  
monasca_checks:
  host_alive:
    init_config:
      ssh_port: 22
      ssh_timeout: 0.5
      ping_timeout: 1
    instances:
      - name: monasca
        host_name: "{{ monasca_host }}"
        alive_test: ssh