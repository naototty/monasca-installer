---
# file: host_vars/openstack-monasca-agent

#Use only supported metrics
monasca_agent_system_only: True

# Monasca Agent Configuration
monasca_agent_user: admin-agent
monasca_agent_password: admin
monasca_agent_project: admin

monasca_agent_version: 1.1.13.dev4
  
monasca_checks:
  host_alive:
    init_config:
      ssh_port: 22
      ssh_timeout: 0.5
      ping_timeout: 1
    instances:
      - name: monasca
        host_name: "{{ monasca_host }}"
        alive_test: ssh
