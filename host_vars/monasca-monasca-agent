---
# file: group_vars/monasca-agent-openstack

# Monasca Agent user credentials
monasca_agent:
  user: csi-agent
  password: password
  project: csi

# Monasca Agent Configuration
monasca_agent_service: monitoring
monasca_checks:
  host_alive:
    init_config:
      ssh_port: 22
      ssh_timeout: 0.5
      ping_timeout: 1
    instances:
      - name: openstack
        host_name: "{{ openstack_host }}"
        alive_test: ssh
