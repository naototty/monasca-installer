---
# Copyright 2016 FUJITSU LIMITED

# Important: Please provide secure passwords below

# group_vars/monasca_group credentials
# used for influxdb_users
influxdb_mon_api_password:
influxdb_mon_persister_password:
# Used for database_users
database_notification_password:
database_monapi_password:
database_thresh_password:

# Keystone user passwords - used in group_vars/openstack_group/keystone_users
## new created users in keystone:
keystone_cmm_operator_user_password:
keystone_cmm_agent_password:
keystone_admin_agent_password:
## existing keystone user. Important: This password must match to an already existing user in keystone. It is used to create new users.
keystone_admin_password:

# Monasca-Agent credentials. used in host_vars/
cmm_monasca_agent_keystone_password:
openstack_monasca_agent_keystone_password:

# Monasca-Log-Agent credentials. used in host_vars/
cmm_monasca_log_agent_keystone_password:
openstack_monasca_log_agent_keystone_password:

# Grafana password. used in group_vars/monasca_group
database_grafana_password:
